{
  "name": "coffeemap",
  "version": "1.0.0",
  "description": "Coffeemap mapbox project",
  "scripts": {
    "pw:install": "bash -c 'if command -v apt-get >/dev/null 2>&1; then npx playwright install-deps || true; fi; npx playwright install chromium'",
    "test": "npm run pw:install && playwright test",
    "build:dataset": "node scripts/build-dataset.mjs",
    "test:prebuilt": "playwright test tests/data-loader.prebuilt.spec.ts",
    "test:runtime": "npm run pw:install && playwright test tests/runtime-features.spec.ts --reporter=line",
    "check:dataset": "node scripts/verify-dataset.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "http-server": "^14.1.1"
  },
  "type": "module"
}
